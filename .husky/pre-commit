#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged to check and fix code quality
npm run pre-commit

# Run type checking
npm run type-check

# Run security audit
npm run security:audit 